# Example notebooks
Put exploratory Jupyter notebooks here (e.g. `eda.ipynb`, `model_comparison.ipynb`). None are checked in by default — this pipeline is fully reproducible from the `.py` scripts alone.
